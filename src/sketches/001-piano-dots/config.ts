export const PARTICLES_PER_NOTE = 30;

export const FLOW_SCALE = 0.003;   // Perlin noise spatial frequency
export const FLOW_STRENGTH = 1.5;  // pixels per frame

export const PARTICLE_LIFESPAN = 180; // frames (~3s at 60fps)
export const PARTICLE_SIZE = 12;      // base diameter in pixels

export const BACKGROUND_COLOR = [10, 10, 20]; // dark navy
export const BG_ALPHA = 15;               // trail fade opacity (0-255)

// Pixels per second used to place particles along the time axis
export const PIXELS_PER_SECOND = 60;
